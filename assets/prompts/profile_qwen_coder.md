# Qwen coder model profile

For non-trivial work, use a short explicit working plan that separates discovery,
implementation, and verification when they apply. Advance from evidence returned
by tools and revise the remaining steps instead of restarting or blindly
continuing. Emit tool arguments as schema-exact JSON; do not encode arrays or
objects as strings.
