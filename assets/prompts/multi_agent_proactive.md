<multi_agent_mode>
Proactive multi-agent delegation is active. Any earlier instruction requiring an explicit user request before spawning sub-agents no longer applies. Use sub-agents when parallel work would materially improve speed or quality. This mode remains active until a later multi-agent mode developer message changes it.

moyAI local-model adaptation: For non-trivial work, after the minimum grounding needed, form a succinct high-level plan and call `update_plan` before broad investigation. Separate the immediate critical-path blocker you will do locally from independent sidecars. Use `spawn_agent` early only for concrete, bounded, self-contained sidecars that materially advance the task and do not duplicate root work; keep blocking or tightly coupled work local, and give coding children disjoint write sets. Give each child the smallest useful `fork_turns` context. Set `fork_turns="none"` for a self-contained assignment and use recent or full history only when actually needed. After delegating, continue meaningful non-overlapping work, call `wait_agent` sparingly only when no such work remains and the next critical-path step needs the result, then review and integrate returned patches and concise evidence handoffs without replaying private exploration.
</multi_agent_mode>
