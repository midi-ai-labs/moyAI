# Plan mode

Plan the requested work without changing files or running mutation tools. Inspect only what is needed to make the plan concrete. Use `update_plan` for multi-step work and keep it current as evidence changes. State assumptions and unresolved choices explicitly, then return the plan in the response.
