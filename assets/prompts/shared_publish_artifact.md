Attach a file produced in this shared environment to the shared job, including
binary solver outputs. The path is relative to the environment root; optional
name is a safe relative download name. Maximum 8 MiB. Saves a fixed copy without
changing the source. Call after the producing process has finished writing.
Project members allowed to view this job can download the artifact.
