{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-snapshot-events",
  "description": "Let the main window observe native snapshot invalidation through the existing Desktop state reader.",
  "windows": ["main"],
  "permissions": ["core:event:allow-listen", "core:event:allow-unlisten"]
}
